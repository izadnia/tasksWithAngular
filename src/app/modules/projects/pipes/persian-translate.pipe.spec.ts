import { PersianTranslatePipe } from './persian-translate.pipe';

describe('PersianTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PersianTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
